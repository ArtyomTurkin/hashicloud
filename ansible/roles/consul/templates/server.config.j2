{
    "bootstrap_expect": {{ consul_servers | length }},
    "server": true,
    "datacenter": "{{ datacenter }}",
    "data_dir": "/var/consul",
    "encrypt": "{{ consul_key }}",
    "log_level": "INFO",
    "enable_syslog": true,
    "retry_join": {{ consul_servers | to_json }}
}